{
  "rules": {
    "users": {
      "$userId": {
        ".read": "auth != null && $userId === auth.uid",
        ".write": "auth != null && $userId === auth.uid",
        "tasks": {
          ".read": "auth != null && $userId === auth.uid",
          ".write": "auth != null && $userId === auth.uid",
          "$taskId": {
            ".read": "auth != null && $userId === auth.uid",
            ".write": "auth != null && $userId === auth.uid",
            "subtasks": {
              ".read": "auth != null && $userId === auth.uid",
              ".write": "auth != null && $userId === auth.uid",
              "$subtaskId": {
                ".read": "auth != null && $userId === auth.uid",
                ".write": "auth != null && $userId === auth.uid"
              }
            }
          }
        },
        "habits": {
          ".read": "auth != null && $userId === auth.uid",
          ".write": "auth != null && $userId === auth.uid",
          "$habitId": {
            ".read": "auth != null && $userId === auth.uid",
            ".write": "auth != null && $userId === auth.uid",
            "completions": {
              ".read": "auth != null && $userId === auth.uid",
              ".write": "auth != null && $userId === auth.uid",
              "$date": {
                ".read": "auth != null && $userId === auth.uid",
                ".write": "auth != null && $userId === auth.uid"
              }
            }
          }
        },
        "goals": {
          ".read": "auth != null && $userId === auth.uid",
          ".write": "auth != null && $userId === auth.uid",
          "$goalId": {
            ".read": "auth != null && $userId === auth.uid",
            ".write": "auth != null && $userId === auth.uid"
          }
        },
        "rewards": {
          ".read": "auth != null && $userId === auth.uid",
          ".write": "auth != null && $userId === auth.uid",
          "$rewardId": {
            ".read": "auth != null && $userId === auth.uid",
            ".write": "auth != null && $userId === auth.uid"
          }
        },
        "points": {
          ".read": "auth != null && $userId === auth.uid",
          ".write": "auth != null && $userId === auth.uid"
        },
        "user_settings": {
          ".read": "auth != null && $userId === auth.uid",
          ".write": "auth != null && $userId === auth.uid"
        }
      }
    },
    ".read": false,
    ".write": false
  }
}
